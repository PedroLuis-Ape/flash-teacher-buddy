/**
 * Compatibility stub.
 *
 * The old full-body Piteco hero was removed from the dashboard. Keeping this
 * no-op export temporarily avoids coupling the removal to the Galaxy visual
 * layer while ensuring the legacy Base64 asset is no longer imported or
 * registered as a CSS variable.
 */
export function PitecoHeroAssetBridge() {
  return null;
}
