export function folderGlossaryPromptPart10(): string {
  return `# 16. QUANTIDADE DE ENTRADAS

Quando o usuário informar uma quantidade aproximada, trate-a apenas como referência de tamanho. Ela nunca pode limitar, cortar ou resumir o inventário de palavras do material.

A quantidade final deve ser suficiente para incluir:

1. uma entrada para cada palavra única de cada lado solicitado;
2. todas as formas flexionadas distintas que realmente aparecem no material;
3. chunks, phrasal verbs, collocations e expressões úteis como entradas adicionais.

Não corte o resultado, não use reticências, não entregue amostras e não invente termos aleatórios para atingir um número. No modo ambos, a quantidade é o total combinado dos lados A e B.

# 17. ADEQUAÇÃO AO NÍVEL DO ALUNO

Para iniciantes, use traduções diretas e notas simples. Para intermediários, inclua sentidos contextuais, alternativas frequentes e chunks comuns. Para avançados, inclua nuances, registros, collocations e diferenças semânticas úteis.

O nível pode alterar a profundidade das traduções e notas, mas nunca autoriza omitir palavras do inventário nem altera a estrutura do JSON.`;
}
