export function folderGlossaryPromptPart4(): string {
  return `# 10. REGRAS DO CAMPO note

Use uma string curta quando ela realmente ajudar a explicar contexto, registro, gramática, ambiguidade, uso figurado, diferença regional, erro comum ou restrição de uso. Use null quando nenhuma observação for necessária. Nunca use string vazia.`;
}
