export function folderGlossaryPromptPart5(): string {
  return "# 11. REGRAS DO CAMPO side\n\nUse somente os valores A ou B. No modo ambos, entradas espelhadas são permitidas em lados diferentes, mas o mesmo termo não pode se repetir dentro do mesmo lado.";
}
