/**
 * Emergency rollback switch for the list-to-Special bulk action.
 * Turning this off hides only the new entry point; individual Special marking
 * and the existing queue remain untouched.
 */
export const BULK_LIST_SPECIALS_ENABLED = true;
