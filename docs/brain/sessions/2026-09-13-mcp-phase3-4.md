---
cssclasses:
  - ape-ai-note
---

# MCP Piteco — FASE 3 + FASE 4 (escrita e destrutivos) — 2026-09-13

## Estado e fonte da verdade

- [FATO CONFIRMADO] Worktree C:\Users\pedro\Documents\App-Piteco-Worktrees\ape-discovery-activation-20260913, branch integration/ape-program-20260913, sem commit/merge/deploy.
- [FATO CONFIRMADO] Base: commit 08bd0be5 (FASE 1+2). Caminhos de outro worker (src/lib/mcp/learning/**, src/lib/mcp/tools/analyzeText.ts, docs/mcp/**) não foram tocados.

## Objetivo

FASE 3 (create/update/move/batch + GATE_WRITE) e FASE 4 (preview +
confirmation token + GATE_DESTRUCTIVE), reutilizando a camada de domínio,
preservando RLS, soft delete e as regras do produto.

## Trabalho executado

- Domínio novo: validation.ts, folderWrites.ts, listWrites.ts, cardWrites.ts,
  trash.ts, confirmation.ts, além de findOwnedFolder, findOwnedList e
  compactFolderSummary em access.ts, ConfirmationKey/createToolIdentity em
  client.ts e o código confirmation_required em errors.ts.
- 15 tools registradas (src/lib/mcp/index.ts, versão 0.3.0): create_folder,
  update_folder, create_list, update_list, move_list, reorder_lists,
  duplicate_list, add_flashcards, update_flashcards, remove_flashcards,
  preview_delete_list, confirm_delete_list, preview_delete_folder,
  confirm_delete_folder, restore_from_trash.
- Testes: domainWriteFoldersLists.test.ts, domainWriteCards.test.ts,
  domainTrash.test.ts, toolsWrite.test.ts (boundary autenticado) e
  toolsReadOnly.test.ts atualizado para a nova superfície; fakeSupabase.ts
  ganhou insert/update/RPCs de lixeira e fixtures.ts passou a clonar tabelas
  por teste.

## Decisões

- [DECISAO VIGENTE] Identidade sempre do token; nenhuma tool aceita user_id, e
  toda escrita filtra por posse (owner_id/folders.owner_id) e system_kind=user.
- [DECISAO VIGENTE] Quem define o escopo de um objeto é o próprio objeto; criar
  exige destino explícito.
- [DECISAO VIGENTE] Confirmação em dois passos com token stateless (HMAC com o
  bearer, TTL 600 s, vinculado à contagem previsualizada).
- [DECISAO VIGENTE] Remoções usam as RPCs de lixeira do produto; hard delete e
  purga continuam fora do MCP.

## Evidência

- [VERIFIED-TEST] Suite focada do MCP: 12 arquivos / 89 testes PASS
  (vitest run nos arquivos do lote).
- [VERIFIED-GATE] tsc app exit 0 e tsc node exit 0; eslint nos arquivos do lote
  exit 0; npm run build exit 0 com APE SEO visibility score 100/100;
  node scripts/brain-check.mjs -> BRAIN_CHECK_PASS (58 notas, 658 wikilinks).
- [VERIFIED-TEST] RED para GREEN do lote: fixtures compartilhadas vazavam
  deleted_at (corrigido com clone por tabela), o fake não aplicava defaults de
  coluna nem sincronizava embeds após escrita, ids gerados colidiam entre
  clients e a superfície de annotations/version quebrou os contratos da
  FASE 1+2 — todos corrigidos até 89/89.
- [NAO VERIFICADO] Nenhuma chamada real ao Supabase/MCP e nenhuma verificação
  de RLS em produção nesta rodada.

## Riscos e limites

- A confirmação destrutiva é vinculada ao bearer da requisição: refresh de
  sessão entre preview e confirmação exige novo preview (falha segura).
- A prova de isolamento entre contas é de domínio (filtros) com fake PostgREST,
  não de RLS real.
- duplicate_list limita a cópia a 2000 cards ativos por chamada e compensa
  falha de lote enviando a cópia parcial para a lixeira.

## Handoff

Próximo passo seguro: smoke autenticado real (create_folder -> create_list ->
add_flashcards -> update_list -> preview/confirm delete -> restore) e, depois,
as fases de análise de texto (dono: outro worker). Área:
[[areas/mcp-agent-api]]. Relatório do lote:
.superpowers/sdd/mcp-phase3-4-report.md.

## Integração com o motor de vocabulário (pedido do supervisor)

- [FATO CONFIRMADO] analyze_text_against_library foi registrado em
  src/lib/mcp/index.ts no grupo read-only, logo após search_my_content, e
  incluído na lista read-only dos testes. Nenhum arquivo de
  src/lib/mcp/learning/** nem tools/analyzeText.ts foi editado.
- [DECISAO VIGENTE] Toda escrita invalida o inventário no fim da operação com
  invalidateScopeInventory(userId, institutionId), usando a MESMA chave do
  motor (userId + "|" + scopeName). Cobre pastas, listas, cards, lixeira e
  restore; dry_run não invalida.
- [FATO CONFIRMADO] Gates finais: vitest run src/lib/mcp = 18 arquivos / 117
  testes PASS (12 arquivos / 89 testes do lote); typecheck app e node exit 0;
  eslint 0 nos arquivos do lote; npm run build exit 0 com SEO 100/100.
- [FATO CONFIRMADO] Os dois erros de typecheck de ContentBlock.text foram
  corrigidos com leitura tipada do content block.
- [RISCO] invalidateVocabularyInventory limpa o Map em memória do isolate:
  outro isolate pode servir inventário com até 60 s de atraso. Ver [[08-RISKS]].
- [FATO CONFIRMADO] O bundle auto-gerado foi restaurado após o build (o plugin
  no Windows emite import npm:C:... inválido para Deno).
