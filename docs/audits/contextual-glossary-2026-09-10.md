# Contextual glossary — implementation checkpoint, NOT release-ready

## Scope and invariants

Preserve base glossary, card-local senses and overlapping expressions. Card hints
without explicit scope are contextual; sync must not promote them automatically.
No LLM during gameplay. No changes to auth, progress, sessions, economy or Home.

## Implemented locally

- Optional backward-compatible hint scope, kind, canonical expression and UTF-16
  discontinuous segments. Smart adapters retain these and both index spellings.
- Occurrence-specific merge identities; stale indexed contextual layers fail closed.
- Segmented expressions highlight only their verb/particle, not intervening objects.
- Sync promotes only explicitly global hints; no existing records are rewritten.
- Folder card export preserves front/back whitespace and camelCase hint indexes.
- Bounded English expression forms/candidates (pending review, not inferred senses).
- Context labels and expandable layers in the existing popover/sheet.
- Shared prompt rules; source export includes both sides and stable card references.
- Coverage examples/review evidence carry the opposite text and contextual fields.
- Additive converter migration generated by Supabase CLI; NOT applied remotely.
- 13 new tests cover adapter round-trip, separate senses/occurrences, discontinuous
  spans, negative candidates, whitespace and sync non-promotion.

## Release blockers and remaining implementation

Production project `ymahldldyxvwjeruaxpr` rejects the connected MCP authorization,
including a retry after the user's permission confirmation. Administrative project
`xrnfhhoxmmstagmelvyi` was checked read-only; its state is NOT production evidence.

This checkpoint is incomplete and must not be merged or published yet:

1. Verify/apply/test converter on the actual data project; validate all import RPC
   paths (some still trim front/back and could move offsets). Gate extended input
   against backend capability to prevent silent loss on an older converter.
2. Finish complete backup of folder/list glossary, alternatives, languages, inactive
   entries, empty lists and layered identities; finish all legacy/CSV converter paths.
3. Finish generation corpus download entry point, contextual revision import/apply
   with ownership and stale-evidence checks, and candidate presentation/review.
4. Finish full-context resolver wiring and semantic display deduplication; retain
   useful base senses without presenting a generic alternative as a resolved sense.
5. Version review extensions and test legacy review file compatibility; ensure
   examples belong to the specific expression, not only its constituent token.
6. Runtime tests after reload/sync/reimport, browser mobile/offline and full npm build
   pipeline. Local adapter serialization is NOT proof of database round-trip.

## Validation at checkpoint

- New focused tests: 13/13 passed.
- Complete suite after restoring the localized validation message: 1524/1524
  tests passed in 241 files; no assertions weakened.
- App TypeScript check passed before final checkpoint; final checks in task output.
- ESLint: zero errors, 72 existing warnings.
- Vite production build passed; this is not the full npm build/prerender pipeline.
- No remote migration, production write, deployment or merge performed.

## Rollback

Changes are isolated on `feat/contextual-glossary-20260910`, based on main
`6da009b1`. Main and production remain untouched. Do not delete existing hints or
global alternatives to roll back; provenance of old alternatives cannot be inferred.
